import { Button } from "@mui/material";

export default function Learn() {
  return (
    <div>
      <h1>Started Journey  -Rock it Buddy</h1>
      <Button variant="contained" color="primary">
        Click Me
      </Button>
    </div>
  );
}